

kde.py:

Use kernel density esitmation method estimate the probability distribution of the request count within a second, and then sample values from the distribution as predictions for the request counts.



cl_reg.py:

Use regression or classification models predict the request counts within a second.



gen_training_data.py:

Generate training dataset for classification and regression models.


#############################################################


How to run ?

0. python ..\gen_request_load.py  -- compute request count within minute or second

1. python gen_training_data.py  -- generate training dataset

2. python kde.py  -- sample based on kerne1 density esitmation

3. python cl_reg.py  -- do classification or regression